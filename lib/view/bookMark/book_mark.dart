import 'package:flutter/material.dart';

class SaveNewsScreen extends StatelessWidget {
  const SaveNewsScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("save news"),
      ),
    );
  }
}