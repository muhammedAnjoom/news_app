import 'package:flutter/material.dart';

class NotflicationScreen extends StatelessWidget {
  const NotflicationScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("notflication"),
      ),
    );
  }
}